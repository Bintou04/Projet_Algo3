## Database Management (Member 2)

This part of the project is responsible for data persistence using MySQL.

### Completed tasks
- Database schema design
- Creation of supports table
- SQL script for database creation
- MySQL connection module in C

### In progress
- Loading supports from database into linked list
- CRUD operations

The database is accessed through a dedicated module (database.c / database.h).
